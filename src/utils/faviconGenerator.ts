// Favicon is now served statically from /favicon.svg — no generation needed.
export const generateAndSetFavicon = async () => {
  // Static favicon configured in index.html, nothing to do at runtime.
};
